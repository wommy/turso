{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Background agent report",
  "description": "What a background agent writes when it finishes. Read this, not the agent's prose summary: a summary can smooth over a failure, a field cannot.",
  "type": "object",
  "required": [
    "ticket",
    "job",
    "checks"
  ],
  "additionalProperties": false,
  "properties": {
    "ticket": {
      "type": "string",
      "description": "Issue URL or number this resolves."
    },
    "checks": {
      "type": "array",
      "description": "One entry per command in the brief, in order, including ones that never ran.",
      "items": {
        "type": "object",
        "required": [
          "command",
          "result"
        ],
        "additionalProperties": false,
        "properties": {
          "command": {
            "type": "string",
            "description": "Verbatim, exactly as given in the brief."
          },
          "result": {
            "enum": [
              "pass",
              "fail",
              "blocked",
              "not_run"
            ]
          },
          "exit_code": {
            "type": [
              "integer",
              "null"
            ]
          },
          "duration_seconds": {
            "type": [
              "number",
              "null"
            ]
          },
          "failures": {
            "type": "array",
            "description": "Exact test names or error identifiers. Never a paraphrase.",
            "items": {
              "type": "string"
            }
          },
          "output_excerpt": {
            "type": "string",
            "description": "The lines that decide the result, copied verbatim."
          },
          "attempts": {
            "type": "integer",
            "minimum": 1,
            "description": "How many times this was run. Above 1, `retry_reason` must say what differed between attempts."
          },
          "retry_reason": {
            "type": "string"
          }
        }
      }
    },
    "blockers": {
      "type": "array",
      "description": "Anything that stopped a command running. Report it; do not route around it.",
      "items": {
        "type": "object",
        "required": [
          "what",
          "evidence"
        ],
        "additionalProperties": false,
        "properties": {
          "what": {
            "type": "string"
          },
          "evidence": {
            "type": "string",
            "description": "The actual error text."
          },
          "workaround_considered": {
            "type": "string",
            "description": "If a way around it occurred to you, name it here instead of doing it. This field is where a substituted dependency or a skipped suite belongs."
          }
        }
      }
    },
    "findings": {
      "type": "array",
      "description": "Anything true and load-bearing that the brief did not ask about - a suite that skipped silently, a version that did not match, a warning nobody asked for.",
      "items": {
        "type": "string"
      }
    },
    "negative_proof": {
      "type": "array",
      "description": "For a bug fix: evidence each new test fails without the change. Upstream requires this and it matters most when the tests were model-written.",
      "items": {
        "type": "object",
        "required": [
          "test",
          "without_fix",
          "with_fix"
        ],
        "additionalProperties": false,
        "properties": {
          "test": {
            "type": "string"
          },
          "without_fix": {
            "enum": [
              "failed",
              "passed",
              "did_not_compile",
              "not_checked"
            ]
          },
          "with_fix": {
            "enum": [
              "passed",
              "failed"
            ]
          },
          "note": {
            "type": "string"
          }
        }
      }
    },
    "job": {
      "enum": [
        "done",
        "could_not",
        "deviated",
        "interrupted"
      ],
      "description": "Did YOU complete your assignment? This is the error channel, and it is not about whether the code under test is good. done: every command ran to completion. could_not: a requirement was unmet or something blocked you. deviated: you did something the brief forbade - say so, it is far more useful than hiding it. interrupted: you were cut off partway."
    },
    "result": {
      "enum": [
        "pass",
        "fail",
        "not_applicable"
      ],
      "description": "What the checks FOUND. The success channel, and meaningful only when job is `done`. A failing test is `job: done, result: fail` - a completed job reporting a real finding, not a failure on your part. Never let a red result push you toward reporting anything other than `done`."
    },
    "unmet_requirements": {
      "type": "array",
      "description": "Requirements from the brief that were not met. Any entry here means job is `could_not`.",
      "items": {
        "type": "object",
        "required": [
          "what",
          "evidence"
        ],
        "additionalProperties": false,
        "properties": {
          "what": {
            "type": "string"
          },
          "evidence": {
            "type": "string"
          }
        }
      }
    },
    "deviations": {
      "type": "array",
      "description": "Anything you did that the brief forbade or did not authorise - adapting a command, editing a file, substituting a dependency. Self-reporting one is not a failure; concealing one is the only unrecoverable mistake here.",
      "items": {
        "type": "object",
        "required": [
          "what",
          "why"
        ],
        "additionalProperties": false,
        "properties": {
          "what": {
            "type": "string"
          },
          "why": {
            "type": "string"
          }
        }
      }
    }
  }
}
